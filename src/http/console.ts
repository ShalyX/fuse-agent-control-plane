export function renderOperatorConsole(): string {
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Fuse Operator Console</title>
<style>
:root{--bg:#08090a;--panel:#0f1011;--elev:#17191b;--ink:#f7f8f8;--ink2:#d0d6e0;--muted:#8a8f98;--faint:#62666d;--line:rgba(255,255,255,.085);--accent:#7170ff;--signal:#27a644;--caution:#e8b339;--danger:#ef5f4a;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--ink);background:var(--bg)}
*{box-sizing:border-box}[hidden]{display:none!important}body{margin:0;background:var(--bg);min-height:100vh}button,input,select,textarea{font:inherit}button{cursor:pointer}a{color:inherit;text-decoration:none}.shell{max-width:1440px;margin:0 auto;padding:0 28px 56px}.nav{height:58px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);position:sticky;top:0;background:rgba(8,9,10,.92);backdrop-filter:blur(16px);z-index:5}.brand{display:flex;align-items:center;gap:10px;font-weight:650;letter-spacing:-.02em}.mark{width:24px;height:24px}.navright{display:flex;align-items:center;gap:10px}.chip{font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;text-transform:uppercase;color:var(--muted);border:1px solid var(--line);border-radius:999px;padding:7px 9px}.chip.live{color:#87d59a}.ghost{background:transparent;color:var(--ink2);border:1px solid var(--line);border-radius:7px;padding:8px 11px}.primary{background:var(--accent);border:1px solid var(--accent);color:white;border-radius:7px;padding:9px 12px;font-weight:650}.primary:disabled{background:#202226;border-color:#2a2d31;color:#70757d;cursor:not-allowed}.hero{padding:54px 0 28px;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:32px;align-items:end}.eyebrow{font:600 11px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--accent)}h1{font-size:clamp(34px,5vw,64px);line-height:.98;letter-spacing:-.055em;margin:13px 0 15px;max-width:760px}.lede{color:var(--muted);max-width:620px;line-height:1.55;margin:0}.session{width:360px;background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:16px}.session h2{font-size:14px;margin:0 0 6px}.session p{font-size:12px;color:var(--muted);line-height:1.5;margin:0 0 13px}.session details{border-top:1px solid var(--line);padding-top:11px}.session summary{font-size:12px;color:var(--ink2);cursor:pointer}.session input{margin-top:10px}.session .primary{width:100%;margin-top:8px}.workspace{display:grid;grid-template-columns:220px minmax(0,1fr) 310px;border:1px solid var(--line);border-radius:12px;overflow:hidden;min-height:670px;background:var(--panel)}
 .readiness{display:grid;grid-template-columns:1.15fr repeat(3,minmax(0,1fr));gap:9px;margin-bottom:18px}.readiness-card{border:1px solid var(--line);background:#0c0d0e;padding:13px 14px;min-height:78px}.readiness-card:first-child{background:linear-gradient(135deg,rgba(113,112,255,.13),rgba(12,13,14,.95) 68%);border-color:rgba(113,112,255,.35)}.readiness-card h3{font-size:12px;margin:0 0 7px}.readiness-card p{margin:0;color:var(--muted);font-size:11px;line-height:1.4}.readiness-state{display:flex;align-items:center;gap:7px;margin-top:9px;color:var(--ink2);font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;text-transform:uppercase}.readiness-state .dot{width:6px;height:6px}.containment{border:1px solid var(--line);background:#0c0d0e;padding:18px;margin-bottom:20px}.containment-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;margin-bottom:18px}.containment h2{font-size:20px;letter-spacing:-.035em;margin:4px 0 5px}.containment p{color:var(--muted);font-size:12px;line-height:1.45;margin:0;max-width:620px}.containment-note{font:600 10px/1.3 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;text-transform:uppercase;color:var(--faint);text-align:right;max-width:160px}.branch-map{display:grid;grid-template-columns:1.2fr repeat(3,1fr);gap:8px;align-items:stretch}.branch-node{border:1px solid var(--line);padding:12px;min-height:82px;background:#0a0b0c}.branch-node.root{border-color:rgba(113,112,255,.5);background:rgba(113,112,255,.07)}.branch-node strong{display:block;font-size:12px;margin-bottom:7px}.branch-node span{display:block;color:var(--muted);font:11px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace}.branch-node .branch-status{color:var(--faint);font-size:10px;letter-spacing:.08em;text-transform:uppercase;margin-top:11px}.branch-node.signal{border-color:rgba(232,179,57,.45)}.branch-node.signal .branch-status{color:var(--caution)}.branch-node.safe{border-color:rgba(39,166,68,.45)}.branch-node.safe .branch-status{color:#87d59a}.section-kicker{font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--faint)}
 @media(max-width:1100px){.readiness{grid-template-columns:repeat(2,minmax(0,1fr))}.readiness-card:first-child{grid-column:1/-1}.setup-steps{grid-template-columns:repeat(3,minmax(0,1fr))}.branch-map{grid-template-columns:repeat(2,minmax(0,1fr))}.branch-node.root{grid-column:1/-1}}
 @media(max-width:680px){.readiness{grid-template-columns:1fr}.readiness-card:first-child{grid-column:auto}.containment-head{display:block}.containment-note{text-align:left;margin-top:12px}.branch-map{grid-template-columns:1fr}.branch-node.root{grid-column:auto}}.rail{padding:20px 14px;border-right:1px solid var(--line)}.rail button{width:100%;text-align:left;background:transparent;border:0;color:var(--muted);padding:9px 10px;border-radius:6px;font-size:13px}.rail button.active{background:var(--elev);color:var(--ink)}.rail-label,.label{font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--faint)}.rail-label{padding:0 10px 10px}.main{padding:24px;min-width:0}.side{border-left:1px solid var(--line);padding:20px 16px}.view[hidden]{display:none!important}.viewhead{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;border-bottom:1px solid var(--line);padding-bottom:18px;margin-bottom:20px}.viewhead h2{font-size:23px;letter-spacing:-.035em;margin:4px 0 5px}.viewhead p{font-size:13px;color:var(--muted);margin:0}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.card{border:1px solid var(--line);border-radius:9px;background:#0c0d0e;padding:16px}.card.full{grid-column:1/-1}.card h3{font-size:14px;margin:0 0 5px}.card>p{color:var(--muted);font-size:12px;line-height:1.45;margin:0 0 15px}.field{display:grid;gap:6px;margin:11px 0}.field label{font-size:11px;color:var(--muted)}input,select,textarea{width:100%;background:#0a0b0c;border:1px solid var(--line);color:var(--ink);border-radius:6px;padding:9px 10px;outline:none;font-size:13px}input:focus,select:focus,textarea:focus{border-color:rgba(113,112,255,.75);box-shadow:0 0 0 3px rgba(113,112,255,.1)}textarea{resize:vertical;min-height:76px}.row{display:grid;grid-template-columns:1fr 1fr;gap:9px}.actions{display:flex;justify-content:flex-end;gap:8px;margin-top:14px}.record{border-top:1px solid var(--line);padding:12px 0}.record:first-of-type{border-top:0}.record strong{display:block;font-size:12px}.record span{display:block;color:var(--muted);font:11px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere}.events{margin-top:12px}.event{padding:11px 0;border-bottom:1px solid var(--line)}.event b{font-size:12px}.event p{color:var(--muted);font-size:11px;line-height:1.45;margin:4px 0 0;overflow-wrap:anywhere}.empty{color:var(--faint);font-size:12px;line-height:1.5;padding:14px 0}.notice{display:none;border:1px solid var(--line);border-left:2px solid var(--accent);background:#0c0d0e;padding:11px 12px;margin-bottom:14px;color:var(--ink2);font-size:12px}.notice.show{display:block}.notice.error{border-left-color:var(--danger)}.secret{font:12px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;overflow-wrap:anywhere;color:#b8d2ff}.statusline{display:flex;align-items:center;gap:8px;font-size:12px;color:var(--muted)}.dot{width:7px;height:7px;border-radius:50%;background:var(--faint)}.dot.on{background:var(--signal)}
.toggle{display:inline-flex;align-items:center;gap:9px;color:var(--ink2)}.toggle input{width:16px;height:16px;margin:0;accent-color:var(--accent)}
@media(max-width:1000px){.workspace{grid-template-columns:170px minmax(0,1fr)}.side{display:none}.hero{grid-template-columns:1fr}.session{width:auto}.grid{grid-template-columns:1fr}.card.full{grid-column:auto}}
@media(max-width:680px){.shell{padding:0 14px 32px}.hero{padding-top:32px}.workspace{display:block}.rail{display:flex;overflow:auto;border-right:0;border-bottom:1px solid var(--line);padding:10px}.rail-label{display:none}.rail button{width:auto;white-space:nowrap}.main{padding:16px}.row{grid-template-columns:1fr}.setup-path-head{display:block}.setup-steps{grid-template-columns:repeat(2,minmax(0,1fr))}.nav .chip{display:none}}
@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}
.hero{grid-template-columns:minmax(0,1fr) minmax(380px,460px);gap:clamp(42px,8vw,120px);padding:72px 0 42px;align-items:start}.hero h1{max-width:620px;font-size:clamp(48px,7vw,92px);line-height:.9}.hero .lede{font-size:15px;max-width:430px}.session{width:auto;border-radius:2px;padding:22px;border-color:rgba(113,112,255,.38);box-shadow:18px 18px 0 rgba(113,112,255,.06)}.session-kicker{color:var(--accent);font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.12em;text-transform:uppercase;margin-bottom:16px}.session h2{font-size:25px;letter-spacing:-.045em;margin-bottom:8px}.session p{font-size:12px;line-height:1.55;max-width:330px}.session .primary{width:100%;margin-top:5px}.session details{margin-top:18px}.readiness-card:first-child{background:#0c0d0e;border-color:var(--line)}.primary{border-radius:3px}.ghost{border-radius:3px}
.setup-path{border:1px solid var(--line);background:#0c0d0e;padding:18px;margin-bottom:20px}.setup-path-head{display:flex;justify-content:space-between;align-items:flex-start;gap:18px;margin-bottom:16px}.setup-path-head h2{font-size:20px;letter-spacing:-.035em;margin:4px 0 5px}.setup-path-head p{color:var(--muted);font-size:12px;line-height:1.45;margin:0;max-width:620px}.setup-steps{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}.setup-step{display:flex;align-items:flex-start;gap:9px;text-align:left;background:#0a0b0c;border:1px solid var(--line);color:var(--ink2);padding:11px 10px;min-height:70px}.setup-step:hover,.setup-step.current{border-color:rgba(113,112,255,.6);background:rgba(113,112,255,.08)}.setup-step.complete{border-color:rgba(39,166,68,.45)}.step-index{font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--faint);letter-spacing:.08em}.setup-step.complete .step-index{color:#87d59a}.step-copy{display:grid;gap:6px;min-width:0}.step-copy strong{font-size:11px}.step-copy small{color:var(--muted);font-size:10px;line-height:1.35}.readiness-state.ready{color:#87d59a}.readiness-state.ready .dot{background:var(--signal)}.readiness-card .actions{justify-content:flex-start;margin:11px 0 0}.readiness-card .actions button{font-size:11px;padding:7px 9px}.quick-inference{margin-top:20px}.quick-inference [data-receipt-status]{display:block;margin-top:10px;color:var(--muted);font:11px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace}.quick-inference [data-receipt-status].success{color:#87d59a}.readiness{grid-template-columns:1.35fr repeat(4,minmax(0,1fr))}@media(max-width:1100px){.readiness{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:680px){.readiness{grid-template-columns:1fr}}</style></head><body><div class="shell">
<style>.run-toolbar{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:end;margin-bottom:12px}.run-summary{border:1px solid var(--line);background:#0a0b0c;color:var(--muted);font:11px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace;padding:11px 12px;margin-bottom:12px}.run-layout{display:grid;grid-template-columns:minmax(0,1fr) minmax(280px,.8fr);gap:12px}.run-record{display:block;width:100%;text-align:left;background:#0a0b0c;color:var(--ink2);border:1px solid var(--line);border-radius:6px;padding:13px;margin-bottom:8px}.run-record:hover,.run-record.selected{border-color:rgba(113,112,255,.65);background:rgba(113,112,255,.08)}.run-record-head{display:flex;justify-content:space-between;gap:12px;align-items:center}.run-record-head strong{font-size:12px}.run-record-head span{font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}.run-record-meta{display:flex;flex-wrap:wrap;gap:8px;margin-top:8px;color:var(--muted);font:11px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace}.run-detail{min-height:250px}.run-detail h3{overflow-wrap:anywhere}.detail-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:16px}.detail-item{border-top:1px solid var(--line);padding-top:9px}.detail-item span{display:block;color:var(--faint);font:600 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.08em;text-transform:uppercase;margin-bottom:6px}.detail-item strong{display:block;font-size:12px;overflow-wrap:anywhere}.integration-code{grid-column:1/-1}.integration-code pre{margin:14px 0 0;overflow:auto;padding:14px;background:#08090a;border:1px solid var(--line);color:#cbd8ff;font:11px/1.65 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre}.integration-code code{font:inherit}.integration-note{color:var(--muted);font-size:12px;line-height:1.5}.integration-link{display:inline-block;margin-top:12px;color:#a8a7ff;font-size:12px}.integration-link:hover{text-decoration:underline}@media(max-width:1100px){.run-layout{grid-template-columns:1fr}}@media(max-width:680px){.run-toolbar{grid-template-columns:1fr}.detail-grid{grid-template-columns:1fr}}</style><nav class="nav"><a class="brand" href="/"><svg class="mark" viewBox="0 0 32 32" fill="none" aria-hidden="true"><path d="M7 5v22M25 5v22M7 10h11M14 22h11" stroke="#f7f8f8" stroke-width="2.2" stroke-linecap="round"/><circle cx="20" cy="10" r="3" fill="#7170ff"/><circle cx="12" cy="22" r="3" fill="#27a644"/></svg>Fuse</a><div class="navright"><span class="chip">Control mode</span><span id="sessionChip" class="chip">No session</span><button id="signOut" class="ghost" hidden>Sign out</button></div></nav>
<section class="hero"><div><div class="eyebrow">Fuse / controlled inference</div><h1>From workspace<br>to receipt.</h1><p class="lede">Give an agent a provider, a hard spending boundary, and one paid call you can account for.</p></div>
<div id="sessionPanel" class="session"><div class="session-kicker">01 / workspace</div><h2>Set the boundary.</h2><p>One provider, one ceiling, one accountable call. Your session is scoped to this tab and clears when you sign out or close it.</p><form id="onboardingForm"><div class="field"><label>Workspace name</label><input name="name" required placeholder="Acme workspace"></div><div class="field"><label>Agent name</label><input name="agentName" required value="default-agent"></div><div class="row"><div class="field"><label>Provider</label><select name="provider"><option value="openrouter" selected>OpenRouter</option></select></div><div class="field"><label>Model</label><input name="model" required value="anthropic/claude-sonnet-4.6"></div></div><div class="field"><label>Provider API key</label><input name="apiKey" type="password" required autocomplete="off"></div><div class="field"><label>Beta invite</label><input name="inviteToken" type="password" required autocomplete="off"></div><div class="row"><div class="field"><label>Input USD / 1M</label><input name="inputUsdPerMillion" required value="3.00"></div><div class="field"><label>Output USD / 1M</label><input name="outputUsdPerMillion" required value="15.00"></div></div><div class="field"><label>Maximum spend, atomic units</label><input name="maximumSpendAtomic" required value="100000"></div><button class="primary" type="submit">Create workspace</button></form><div id="onboardingResult" class="notice" hidden></div><details><summary>Restore existing admin session</summary><input id="sessionToken" type="password" autocomplete="off" placeholder="fuse_sk_…" aria-label="Service credential"><input id="sessionAgentToken" type="password" autocomplete="off" placeholder="fuse_sk_… (agent, optional)" aria-label="Agent credential"><button id="continueSession" class="primary">Continue session</button></details><details><summary>Recover an existing beta workspace</summary><p>Use a fresh recovery invite issued by the operator to recover the last workspace when its one-time credentials were lost.</p><input id="workspaceRecoveryInvite" type="password" autocomplete="off" placeholder="fuse_recovery_…" aria-label="Beta recovery invite"><button id="recoverWorkspace" class="primary">Recover workspace access</button><div id="workspaceRecoveryResult" class="secret"></div></details></div></section>
<div id="workspace" class="workspace" hidden><aside class="rail"><div class="rail-label">Operate</div><button data-view="overview" class="active">Overview</button><button data-view="providers">Provider</button><button data-view="agents">Agents</button><button data-view="policy">Policy</button><button data-view="mandate">Mandate</button><button data-view="runs">Runs</button><button data-view="evidence">Evidence</button><button data-view="integration">Integration</button></aside>
<main class="main"><div id="notice" class="notice" role="status"></div>
<section class="view" data-page="overview"><div class="section-kicker">Workspace readiness</div><div id="readinessStrip" class="readiness"><div class="readiness-card"><h3>Control path</h3><p>The workspace bootstraps a provider, policy, mandate, and agent credential. Use the path below to inspect the boundary, then run one call.</p><div class="actions"><button class="primary" type="button" data-go-inference>Run first call</button><button class="ghost" type="button" data-go-provider>Inspect provider</button></div><div class="readiness-state"><span class="dot"></span><span id="readinessSetup">Checking setup…</span></div></div><div class="readiness-card"><h3>Provider</h3><p>Customer-owned credential is stored behind the tenant boundary.</p><div class="readiness-state"><span class="dot"></span><span id="readinessProvider">Checking…</span></div></div><div class="readiness-card"><h3>Agent access</h3><p>Scoped runtime credential is required for inference.</p><div class="readiness-state"><span class="dot"></span><span id="readinessAgent">Checking…</span></div></div><div class="readiness-card"><h3>Authority</h3><p>Policy and mandate define the call ceiling.</p><div class="readiness-state"><span class="dot"></span><span id="readinessAuthority">Checking…</span></div></div><div class="readiness-card"><h3>Evidence</h3><p>Every completed call can be read back as a receipt.</p><div class="readiness-state"><span class="dot"></span><span id="readinessEvidence">After first call</span></div></div></div><div id="setupPath" class="setup-path" aria-labelledby="setupPathTitle"><div class="setup-path-head"><div><div class="section-kicker">Control path</div><h2 id="setupPathTitle">From provider to receipt.</h2><p>Each step has one job. Select a step to inspect or change it; the first incomplete step is highlighted for you.</p></div><div id="setupPathSummary" class="statusline"><span class="dot"></span><span data-path-summary>Checking setup…</span></div></div><div class="setup-steps"><button type="button" class="setup-step" data-step="provider" data-step-view="providers"><span class="step-index">01</span><span class="step-copy"><strong>Provider</strong><small data-step-status>Checking…</small></span></button><button type="button" class="setup-step" data-step="agent" data-step-view="agents"><span class="step-index">02</span><span class="step-copy"><strong>Agent access</strong><small data-step-status>Checking…</small></span></button><button type="button" class="setup-step" data-step="policy" data-step-view="policy"><span class="step-index">03</span><span class="step-copy"><strong>Policy</strong><small data-step-status>Checking…</small></span></button><button type="button" class="setup-step" data-step="mandate" data-step-view="mandate"><span class="step-index">04</span><span class="step-copy"><strong>Mandate</strong><small data-step-status>Checking…</small></span></button><button type="button" class="setup-step" data-step="inference" data-step-inference><span class="step-index">05</span><span class="step-copy"><strong>First call</strong><small data-step-status>Waiting</small></span></button><button type="button" class="setup-step" data-step="receipt" data-step-evidence><span class="step-index">06</span><span class="step-copy"><strong>Receipt</strong><small data-step-status>After first call</small></span></button></div></div><div class="containment"><div class="containment-head"><div><div class="section-kicker">Branch containment</div><h2>One branch can stop without stopping the system.</h2><p>Fuse keeps the root authority visible, assigns each workload its own boundary, and makes the failure mode legible before a paid request leaves the runtime.</p></div><div class="containment-note">No live values until this workspace is connected</div></div><div class="branch-map"><div class="branch-node root"><strong>Root mandate</strong><span>Shared authority boundary</span><span class="branch-status">Not configured</span></div><div class="branch-node"><strong>Scout</strong><span>Discovery workload</span><span class="branch-status">Waiting</span></div><div class="branch-node"><strong>Builder</strong><span>Execution workload</span><span class="branch-status">Waiting</span></div><div class="branch-node"><strong>Reviewer</strong><span>Verification workload</span><span class="branch-status">Waiting</span></div></div></div></section>
 <section class="view" data-page="providers" hidden><header class="viewhead"><div><div class="label">Provider boundary</div><h2>Customer-owned billing</h2><p>Credentials are encrypted at rest and resolved only inside the authenticated tenant path.</p></div><button class="ghost" data-refresh="providers">Refresh</button></header><div class="grid"><form id="providerForm" class="card"><h3>Configure provider</h3><p>Saving a new secret rotates the credential version and removes the prior ciphertext.</p><div class="field"><label>Configuration ID</label><input name="configId" value="primary" required></div><div class="row"><div class="field"><label>Provider</label><select name="provider"><option value="openrouter" selected>OpenRouter</option></select></div><div class="field"><label>Model</label><input name="model" value="anthropic/claude-sonnet-4.6" required></div></div><div class="field"><label>Provider API key</label><input name="apiKey" type="password" autocomplete="new-password" required></div><div class="row"><div class="field"><label>Input USD / 1M</label><input name="inputUsdPerMillion" value="3.00" required></div><div class="field"><label>Output USD / 1M</label><input name="outputUsdPerMillion" value="15.00" required></div></div><div class="actions"><button class="primary">Save provider</button></div></form><div class="card"><h3>Active configurations</h3><p>Metadata only. Provider secrets are never returned by this API.</p><div id="providerList" class="empty">No provider configuration loaded.</div></div></div></section>
<section class="view" data-page="agents" hidden><header class="viewhead"><div><div class="label">Agent identity</div><h2>Scoped runtime access</h2><p>Register an agent, then issue the minimum credential required for inference.</p></div></header><div class="grid"><form id="agentForm" class="card"><h3>Register agent</h3><div class="field"><label>Agent ID</label><input name="agentId" required></div><div class="field"><label>Display name</label><input name="name" required></div><div class="actions"><button class="primary">Register agent</button></div></form><form id="credentialForm" class="card"><h3>Issue agent credential</h3><div class="field"><label>Credential ID</label><input name="credentialId" required></div><div class="field"><label>Agent ID</label><input name="agentId" required></div><div class="field"><label>Credential name</label><input name="name" value="Inference runtime" required></div><div class="actions"><button class="primary">Issue once</button></div><div id="issuedCredential" class="secret"></div></form><div class="card full"><h3>Recover workspace access</h3><p>The original one-time credentials cannot be redisplayed. Because this admin session is still active, you can issue a replacement service credential, agent credential, and recovery code for this workspace. Save the values before leaving this tab.</p><div class="actions"><button id="issueWorkspaceCredentials" class="primary" type="button">Issue replacement access</button></div><div id="workspaceCredentialsResult" class="secret"></div></div></div></section>
<section class="view" data-page="policy" hidden><header class="viewhead"><div><div class="label">Versioned control</div><h2>Publish a policy</h2><p>Hard ceilings remain authoritative. Behavioral ranges run in shadow and cannot deny.</p></div></header><form id="policyForm" class="card full"><div class="row"><div class="field"><label>Policy ID</label><input name="policyId" required></div><div class="field"><label>Version</label><input name="version" type="number" min="1" value="1" required></div></div><div class="row"><div class="field"><label>Provider</label><input name="allowedProvider" value="openrouter" required></div><div class="field"><label>Model</label><input name="allowedModel" value="anthropic/claude-sonnet-4.6" required></div></div><div class="row"><div class="field"><label>Max per call, micro-USD</label><input name="maxPerCallAtomic" value="10000" required></div><div class="field"><label>Max daily, micro-USD</label><input name="maxDailyAtomic" value="250000" required></div></div><div class="row"><div class="field"><label>Max input tokens</label><input name="maxInputTokens" type="number" value="20000" required></div><div class="field"><label>Max output tokens</label><input name="maxOutputTokens" type="number" value="4000" required></div></div><div class="field"><label class="toggle"><input name="includeWorkload" type="checkbox" value="yes"> Enable workload-shadow capability</label><p>Leave off during the compatibility deployment; the policy remains legacy and ceiling-only.</p></div><div class="row"><div class="field"><label>Workload capability</label><input name="workloadClass" value="lookup" pattern="[a-z][a-z0-9_.-]{0,63}" required></div><div class="field"><label>Class invocation ceiling</label><input name="maxClassInvocations" type="number" min="1" value="20" required></div></div><div class="row"><div class="field"><label>Class max per call, micro-USD</label><input name="maxClassPerCallAtomic" value="10000" required></div><div class="field"><label>Class branch budget, micro-USD</label><input name="classBudgetAtomic" value="60000" required></div></div><div class="row"><div class="field"><label>Shadow prior window spend</label><input name="classPriorWindowSpendAtomic" value="3000" required></div><div class="field"><label>Shadow divergence threshold, bps</label><input name="divergenceThresholdBps" type="number" min="10001" value="30000" required></div></div><div class="actions"><button class="primary">Publish policy</button></div></form></section>
<section class="view" data-page="mandate" hidden><header class="viewhead"><div><div class="label">Delegated authority</div><h2>Activate a branch mandate</h2><p>Create in draft, assign the agent, bind an immutable workload branch, then activate.</p></div></header><form id="mandateForm" class="card full"><div class="row"><div class="field"><label>Mandate ID</label><input name="mandateId" required></div><div class="field"><label>Name</label><input name="name" required></div></div><div class="row"><div class="field"><label>Policy ID</label><input name="policyId" required></div><div class="field"><label>Policy version</label><input name="policyVersion" type="number" value="1" min="1" required></div></div><div class="row"><div class="field"><label>Agent ID</label><input name="agentId" required></div><div class="field"><label>Maximum spend, micro-USD</label><input name="maximumSpendAtomic" value="250000" required></div></div><div class="row"><div class="field"><label>Branch ID</label><input name="branchId" required></div><div class="field"><label>Parent branch ID</label><input name="parentBranchId" placeholder="Optional for a root branch"></div></div><div class="row"><div class="field"><label>Authorized workload capability</label><input name="workloadClass" value="lookup" pattern="[a-z][a-z0-9_.-]{0,63}" required></div><div class="field"><label>Branch lifetime budget, micro-USD</label><input name="branchMaximumSpendAtomic" value="60000" required></div></div><div class="field"><label>Branch expiry (optional ISO time)</label><input name="branchExpiresAt" type="datetime-local"></div><div class="actions"><button id="createMandate" type="button" class="ghost">1 · Create draft</button><button id="assignMandateAgent" type="button" class="ghost">2 · Assign agent</button><button id="createMandateBranch" type="button" class="ghost">3 · Bind branch</button><button id="activateMandate" type="button" class="primary">4 · Activate</button></div><div id="delegationEvidence" class="secret"></div></form></section>
<section class="view" data-page="runs" hidden><header class="viewhead"><div><div class="label">Run explorer</div><h2>Every call, explained</h2><p>Inspect durable receipts for this workspace. Costs below are provider usage and Fuse accounting state, not wallet settlement.</p></div><button id="refreshRuns" class="ghost" type="button">Refresh</button></header><div class="run-toolbar"><div class="field"><label>Mandate ID</label><input id="runsMandateId" required placeholder="Active mandate"></div><button id="loadRuns" class="primary" type="button">Load runs</button></div><div id="runSummary" class="run-summary">No run history loaded.</div><div class="run-layout"><div id="runList" class="events"><div class="empty">Load a mandate to inspect its receipts.</div></div><article id="runDetail" class="card run-detail"><h3>Select a run</h3><p>Choose a receipt to inspect its decision, policy version, cost reservation, provider usage, and reconciliation state.</p></article></div></section>
<section class="view" data-page="evidence" hidden><header class="viewhead"><div><div class="label">Decision evidence</div><h2>Inspect authority and shadow outcomes</h2><p>Every decision shows its exact policy version and remaining loss boundary. Shadow signals never alter admission.</p></div><button id="loadReconciliation" class="ghost">Reconciliation queue</button></header><form id="evidenceForm" class="card"><div class="field"><label>Mandate ID</label><input name="mandateId" required></div><div class="actions"><button class="primary">Load decisions</button><button id="loadShadow" type="button" class="ghost">Load sibling shadow</button></div></form><form id="reconciliationForm" class="card"><h3>Resolve a reconciliation hold</h3><div class="row"><div class="field"><label>Request ID</label><input name="requestId" required></div><div class="field"><label>Resolution</label><select name="resolution"><option value="settle">Confirm provider usage</option><option value="confirm_not_billed">Confirm not billed</option></select></div></div><div class="field"><label>Actual cost, micro-USD (required when confirming usage)</label><input name="actualCostAtomic" pattern="\\d*" inputmode="numeric"></div><div class="field"><label>Evidence note</label><textarea name="note" required minlength="1" maxlength="2000"></textarea></div><div class="field"><label>External evidence reference</label><input name="externalReference" required maxlength="512"></div><button class="primary">Resolve hold</button></form><div id="decisionList" class="events"><div class="empty">Enter a mandate ID to inspect its decisions.</div></div></section>
<section class="view" data-page="integration" hidden><header class="viewhead"><div><div class="label">Developer path</div><h2>Ship one bounded call</h2><p>Use the agent credential from this workspace in a server-side TypeScript process. OpenRouter remains the provider; Fuse remains the control boundary.</p></div></header><div class="grid"><div class="card integration-code"><h3>TypeScript quickstart</h3><p class="integration-note">This snippet loads the active mandate, makes one idempotent inference request, and reads back the durable receipt. It never retries an uncertain execution.</p><pre id="integrationSnippet"><code>import { createFuseClient } from '@fuse/fuse-client';&#10;&#10;const fuse = createFuseClient({&#10;  baseUrl: process.env.FUSE_BASE_URL ?? 'https://fuse-agent-control-plane.vercel.app',&#10;  credential: process.env.FUSE_AGENT_CREDENTIAL!,&#10;});&#10;&#10;const workspace = await fuse.workspaceContext();&#10;const requestId = crypto.randomUUID();&#10;const { result, receipt } = await fuse.inferenceWithReceipt({&#10;  mandateId: workspace.mandateId,&#10;  requestId,&#10;  model: workspace.model!,&#10;  maxTokens: 256,&#10;  messages: [{ role: 'user', content: 'Return a one-line status update.' }],&#10;});&#10;&#10;console.log(result.response);&#10;console.log(receipt.executionStatus, receipt.actualCostAtomic);</code></pre><div class="actions"><button id="copyIntegrationSnippet" class="primary" type="button">Copy quickstart</button></div><a class="integration-link" href="https://github.com/ShalyX/fuse-agent-control-plane/blob/main/docs/integration-quickstart.md" target="_blank" rel="noreferrer">Read the full integration guide ↗</a></div><div class="card"><h3>Credentials</h3><p class="integration-note">Use the agent credential for runtime calls. Keep the service credential for restoring the operator session. Neither belongs in a client-side bundle.</p></div><div class="card"><h3>Failure contract</h3><p class="integration-note">Policy denials are safe stops. REQUEST_IN_PROGRESS, REQUEST_REQUIRES_REVIEW, and provider failures require inspection; they are not automatic retry instructions.</p></div></div></section>
</main><aside class="side"><div class="label">Session</div><div class="record"><strong id="principalName">Not connected</strong><span id="principalOrg">Organization unavailable</span></div><div class="label" style="margin-top:18px">Activity</div><div id="activity" class="events"><div class="empty">Administrative actions will appear here.</div></div></aside></div>
</div><script>
const state={token:'',agentToken:'',principal:null,agentId:'',policyId:'',providerConfigId:'',productMandateId:'',model:'',lastInferenceRequestId:'',readiness:null};const runState={receipts:[],selected:null};
const $=(s)=>document.querySelector(s);const all=(s)=>[...document.querySelectorAll(s)];
const sessionAgentField=$('#sessionAgentToken');if(sessionAgentField){sessionAgentField.placeholder='fuse_sk_… (required for inference)';sessionAgentField.setAttribute('aria-label','Agent credential (required for inference)')}
function notice(message,error=false){const workspace=$('#workspace');const node=workspace&&!workspace.hidden?$('#notice'):$('#onboardingResult');if(!node)return;node.hidden=false;node.textContent=message;node.className='notice show'+(error?' error':'');setTimeout(()=>node.classList.remove('show'),5000)}
function activity(title,detail){const box=$('#activity');if(box.querySelector('.empty'))box.innerHTML='';const row=document.createElement('div');row.className='event';const b=document.createElement('b');b.textContent=title;const p=document.createElement('p');p.textContent=detail;row.append(b,p);box.prepend(row)}
 function requestId(prefix){return prefix+'-'+Date.now()+'-'+Math.random().toString(16).slice(2)}
 function wireCopyButtons(){all('[data-copy]').forEach(button=>{button.onclick=async()=>{const target=$(button.dataset.copy);if(!target)return;try{await navigator.clipboard.writeText(target.value);button.textContent='Copied';setTimeout(()=>button.textContent='Copy',1600)}catch{target.focus();target.select();notice('Copy failed; press Ctrl+C to copy the selected value.',true)}}})}
function persistSession(){try{sessionStorage.setItem('fuse.session.token',state.token);sessionStorage.setItem('fuse.session.agentToken',state.agentToken);sessionStorage.setItem('fuse.session.agentId',state.agentId);sessionStorage.setItem('fuse.session.policyId',state.policyId);sessionStorage.setItem('fuse.session.providerConfigId',state.providerConfigId);sessionStorage.setItem('fuse.session.mandateId',state.productMandateId);sessionStorage.setItem('fuse.session.model',state.model);sessionStorage.setItem('fuse.session.lastInferenceRequestId',state.lastInferenceRequestId)}catch{} }
function clearPersistedSession(){try{sessionStorage.removeItem('fuse.session.token');sessionStorage.removeItem('fuse.session.agentToken');sessionStorage.removeItem('fuse.session.agentId');sessionStorage.removeItem('fuse.session.policyId');sessionStorage.removeItem('fuse.session.providerConfigId');sessionStorage.removeItem('fuse.session.mandateId');sessionStorage.removeItem('fuse.session.model');sessionStorage.removeItem('fuse.session.lastInferenceRequestId')}catch{} }
function showWorkspace(principal,keepSessionPanel=false){state.principal=principal;$('#workspace').hidden=false;if(!keepSessionPanel)$('#sessionPanel').hidden=true;$('#signOut').hidden=false;$('#sessionChip').textContent=(principal.role||'admin')+' session';$('#sessionChip').classList.add('live');$('#principalName').textContent=(principal.role||'admin')+' session';$('#principalOrg').textContent=principal.organizationId||'Organization unavailable'}
function hydrateSetupForms(context){const values={'#providerForm input[name="configId"]':context.providerConfigId,'#providerForm input[name="model"]':context.model,'#credentialForm input[name="agentId"]':context.agentId};Object.entries(values).forEach(([selector,value])=>{const input=$(selector);if(input&&value)input.value=value});const evidenceMandate=$('#evidenceForm input[name="mandateId"]');if(evidenceMandate&&context.mandateId)evidenceMandate.value=context.mandateId;const runsMandate=$('#runsMandateId');if(runsMandate&&context.mandateId)runsMandate.value=context.mandateId}
async function loadWorkspaceContext(){try{const context=await api('/api/v1/product/workspace-context');state.agentId=context.agentId||state.agentId;state.policyId=context.policyId||state.policyId;state.providerConfigId=context.providerConfigId||state.providerConfigId;state.productMandateId=context.mandateId||state.productMandateId;state.model=context.model||state.model;persistSession();hydrateSetupForms(context)}catch{}}
async function activateWorkspace(principal,keepSessionPanel=false){showWorkspace(principal,keepSessionPanel);await loadWorkspaceContext();await loadReadiness();await loadProviders();ensureQuickInference();await loadRuns();if(!keepSessionPanel)$('#workspace').scrollIntoView({behavior:'smooth',block:'start'})}
async function restoreStoredSession(){let token='',agentToken='',agentId='',policyId='',providerConfigId='',mandateId='',model='',lastInferenceRequestId='';try{token=sessionStorage.getItem('fuse.session.token')||'';agentToken=sessionStorage.getItem('fuse.session.agentToken')||'';agentId=sessionStorage.getItem('fuse.session.agentId')||'';policyId=sessionStorage.getItem('fuse.session.policyId')||'';providerConfigId=sessionStorage.getItem('fuse.session.providerConfigId')||'';mandateId=sessionStorage.getItem('fuse.session.mandateId')||'';model=sessionStorage.getItem('fuse.session.model')||'';lastInferenceRequestId=sessionStorage.getItem('fuse.session.lastInferenceRequestId')||''}catch{}if(!token)return;state.token=token;state.agentToken=agentToken;state.agentId=agentId;state.policyId=policyId;state.providerConfigId=providerConfigId;state.productMandateId=mandateId;state.model=model;state.lastInferenceRequestId=lastInferenceRequestId;try{const identity=await api('/api/v1/identity');await activateWorkspace(identity);activity('Session restored',identity.organizationId)}catch{state.token='';state.agentToken='';state.agentId='';state.policyId='';state.providerConfigId='';state.productMandateId='';state.model='';state.lastInferenceRequestId='';clearPersistedSession()}}
async function api(path,options={}){const headers={...(options.headers||{}),Authorization:'Bearer '+state.token};if(options.body){headers['Content-Type']='application/json';headers['X-Request-Id']=requestId('console')}const response=await fetch(path,{...options,headers,cache:'no-store'});let body={};try{body=await response.json()}catch{}if(!response.ok)throw new Error(body?.error?.code||('HTTP_'+response.status));return body}
function readinessLabel(value){return value==='verified'?'Verified':value==='configured'?'Configured':value==='not_applicable'?'Not used':'Needs setup'}
function readinessReady(value){return value==='verified'||value==='configured'}
function setReadinessState(selector,value,customLabel){const text=$(selector);if(!text)return;text.textContent=customLabel||readinessLabel(value);const stateNode=text.closest('.readiness-state');if(!stateNode)return;const ready=readinessReady(value);stateNode.classList.toggle('ready',ready);const dot=stateNode.querySelector('.dot');if(dot)dot.classList.toggle('on',ready)}
function updateSetupPath(result){const checks=result?.checks||{};const providerReady=readinessReady(checks.provider);const agentReady=readinessReady(checks.agentCredential)&&Boolean(state.agentToken);const authorityReady=readinessReady(checks.policy)&&readinessReady(checks.mandate);setReadinessState('#readinessProvider',checks.provider);setReadinessState('#readinessAgent',agentReady?'configured':'unavailable',agentReady?'Ready':'Paste agent credential');setReadinessState('#readinessAuthority',authorityReady?'configured':'unavailable',authorityReady?'Ready':'Needs policy + mandate');setReadinessState('#readinessEvidence',state.lastInferenceRequestId?'configured':'unavailable',state.lastInferenceRequestId?'Receipt available':'After first call');const steps=[{key:'provider',ready:providerReady,label:readinessLabel(checks.provider)},{key:'agent',ready:agentReady,label:agentReady?'Ready':'Paste agent credential'},{key:'policy',ready:readinessReady(checks.policy),label:readinessLabel(checks.policy)},{key:'mandate',ready:readinessReady(checks.mandate),label:readinessLabel(checks.mandate)},{key:'inference',ready:Boolean(state.lastInferenceRequestId),label:state.lastInferenceRequestId?'Completed':'Run one bounded call'},{key:'receipt',ready:Boolean(state.lastInferenceRequestId),label:state.lastInferenceRequestId?'Available':'Read back after call'}];const next=steps.find(step=>!step.ready);steps.forEach(step=>{const node=document.querySelector('[data-step="'+step.key+'"]');if(!node)return;node.classList.toggle('complete',step.ready);node.classList.toggle('current',Boolean(next&&next.key===step.key));const status=node.querySelector('[data-step-status]');if(status)status.textContent=step.label});const nextText=next?(next.key==='provider'?'Inspect provider':next.key==='agent'?'Add the agent credential':next.key==='policy'?'Publish policy':next.key==='mandate'?'Review mandate':next.key==='inference'?'Run first bounded inference':'Read the receipt'):'Control path verified';$('#readinessSetup').textContent=nextText;const summary=$('#setupPathSummary');if(summary){const text=summary.querySelector('[data-path-summary]');if(text)text.textContent=nextText;const dot=summary.querySelector('.dot');if(dot)dot.classList.toggle('on',!next)}}
async function loadReadiness(){try{const result=await api('/api/v1/product/readiness');state.readiness=result;updateSetupPath(result);$('.containment-note').textContent=result.status==='ready'?'Readiness verified':(result.missingSteps.join(' · ')||'Setup is required before live values appear')}catch(e){notice('Readiness unavailable',true)}}

async function createWorkspaceRequest(e){e.preventDefault();const form=e.currentTarget;const result=$('#onboardingResult');const data=formObject(form);const inviteToken=data.inviteToken;delete data.inviteToken;const key='console-'+Date.now()+'-'+Math.random().toString(36).slice(2,10);try{const payload=await fetch('/api/v1/product/workspaces',{method:'POST',headers:{'Content-Type':'application/json','Idempotency-Key':key,'X-Fuse-Invite':inviteToken},body:JSON.stringify(data)}).then(async r=>{const v=await r.json();if(!r.ok)throw new Error(r.status===429?'ONBOARDING_RATE_LIMIT_EXCEEDED':(v.error?.code||'WORKSPACE_CREATION_FAILED'));return v});const sessionToken=await exchangeSession(payload.adminCredential.token);state.token=sessionToken;state.agentToken=payload.credential.token;state.productMandateId=payload.mandateId;state.principal={organizationId:payload.workspaceId,role:'admin'};form.hidden=true;result.hidden=false;result.className='notice show';result.innerHTML='<strong>Workspace ready.</strong> Admin session established. Save these one-time credentials before leaving this tab.<div class="field"><label>Service credential — restore admin session</label><input id="onboardingServiceCredential" type="text" value="'+payload.adminCredential.token+'" readonly><button class="ghost" type="button" data-copy="#onboardingServiceCredential">Copy</button></div><div class="field"><label>Agent credential — run inference</label><input id="onboardingAgentCredential" type="text" value="'+payload.credential.token+'" readonly><button class="ghost" type="button" data-copy="#onboardingAgentCredential">Copy</button></div><div class="field"><label>Recovery code</label><input id="onboardingRecoveryCode" type="text" value="'+(payload.recoveryCode||'')+'" readonly><button class="ghost" type="button" data-copy="#onboardingRecoveryCode">Copy</button></div><p>Keep the service credential for admin restore and the agent credential for inference. These values are shown once.</p><form id="quickInference" class="quick-inference"><div class="field"><label>First bounded inference</label><textarea name="prompt" required placeholder="Ask a small, bounded question"></textarea></div><button class="primary" type="submit">Run bounded inference</button><span id="quickInferenceStatus"></span></form>';wireCopyButtons();document.querySelector('#quickInference').onsubmit=async(ev)=>{ev.preventDefault();const prompt=new FormData(ev.currentTarget).get('prompt');const status=$('#quickInferenceStatus');const inferenceRequestId='infer-'+Date.now()+'-'+Math.random().toString(36).slice(2,10);const readBackReceipt=async()=>{const receiptResponse=await fetch('/api/v1/product/receipts/'+encodeURIComponent(inferenceRequestId),{headers:{Authorization:'Bearer '+state.agentToken,'X-Fuse-Mandate':payload.mandateId},cache:'no-store'});const receiptBody=await receiptResponse.json().catch(()=>({}));if(!receiptResponse.ok)throw new Error(receiptBody.error?.code||'RECEIPT_READBACK_FAILED');const receipt=receiptBody.receipt;status.textContent='Receipt read back: '+receipt.requestId+' · '+receipt.executionStatus+' · reserved '+receipt.reservedCostAtomic+' · actual '+(receipt.actualCostAtomic??'unresolved');};try{const r=await fetch('/api/v1/product/inference',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+state.agentToken,'Idempotency-Key':inferenceRequestId,'X-Fuse-Mandate':payload.mandateId},body:JSON.stringify({model:data.model,messages:[{role:'user',content:String(prompt)}],max_tokens:256})});const v=await r.json();if(r.status===402)throw new Error('CONTROL_MODE_PAYMENT_UNEXPECTED');if(!r.ok)throw new Error(v.error?.code||'INFERENCE_FAILED');await readBackReceipt();}catch(err){status.textContent=err.message}};result.scrollIntoView({behavior:'smooth',block:'center'});}catch(err){result.hidden=false;result.className='notice show error';result.textContent=err.message;result.scrollIntoView({behavior:'smooth',block:'center'});notice(err.message,true)}}
async function exchangeSession(credential){const response=await fetch('/api/v1/session',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+credential},body:JSON.stringify({expiresAt:new Date(Date.now()+86400000).toISOString()}),cache:'no-store'});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body?.error?.code||'SESSION_EXCHANGE_FAILED');return body.token}
async function runBoundedInference(e){e.preventDefault();const form=e.currentTarget;const prompt=String(new FormData(form).get('prompt')||'').trim();const status=form.querySelector('#quickInferenceStatus')||form.querySelector('[data-inference-status]');if(!prompt){form.reportValidity();return}if(!state.agentToken||!state.productMandateId){if(status)status.textContent='AGENT_SESSION_REQUIRED';notice('Agent credential and mandate are required for inference.',true);return}const model=state.model.trim();if(!model){if(status)status.textContent='PROVIDER_MODEL_REQUIRED';notice('Save a provider model before running inference.',true);return}const inferenceRequestId=requestId('infer');if(status)status.textContent='Running bounded inference…';try{const response=await fetch('/api/v1/product/inference',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+state.agentToken,'Idempotency-Key':inferenceRequestId,'X-Fuse-Mandate':state.productMandateId},body:JSON.stringify({model,messages:[{role:'user',content:prompt}],max_tokens:256}),cache:'no-store'});const body=await response.json().catch(()=>({}));if(response.status===402)throw new Error('CONTROL_MODE_PAYMENT_UNEXPECTED');if(!response.ok)throw new Error(body?.error?.code||'INFERENCE_FAILED');const receiptResponse=await fetch('/api/v1/product/receipts/'+encodeURIComponent(inferenceRequestId),{headers:{Authorization:'Bearer '+state.agentToken,'X-Fuse-Mandate':state.productMandateId},cache:'no-store'});const receiptBody=await receiptResponse.json().catch(()=>({}));if(!receiptResponse.ok)throw new Error(receiptBody?.error?.code||'RECEIPT_READBACK_FAILED');const receipt=receiptBody.receipt;state.lastInferenceRequestId=receipt.requestId;persistSession();if(status)status.textContent='Receipt read back: '+receipt.requestId+' · '+receipt.executionStatus+' · reserved '+receipt.reservedCostAtomic+' · actual '+(receipt.actualCostAtomic??'unresolved');updateSetupPath(state.readiness);await loadRuns();activity('Bounded inference completed',inferenceRequestId)}catch(error){const message=error instanceof Error?error.message:'INFERENCE_FAILED';if(status)status.textContent=message;notice(message,true)}}
function ensureQuickInference(){let form=$('#quickInference');if(!form){const overview=$('[data-page="overview"]');if(!overview)return;form=document.createElement('form');form.id='quickInference';form.className='card quick-inference';form.innerHTML='<h3>First bounded inference</h3><p>Run one provider call inside the active mandate and read its receipt back immediately.</p><div class="field"><label>Prompt</label><textarea name="prompt" required placeholder="Ask a small, bounded question"></textarea></div><button class="primary" type="submit">Run bounded inference</button><span data-inference-status aria-live="polite"></span>';overview.append(form)}form.onsubmit=runBoundedInference}
async function connect(token,agentToken=''){const credential=token.trim();if(!credential)throw new Error('CREDENTIAL_REQUIRED');const sessionToken=await exchangeSession(credential);state.token=sessionToken;state.agentToken=agentToken.trim();const identity=await api('/api/v1/identity');persistSession();$('#sessionToken').value='';$('#sessionAgentToken').value='';await activateWorkspace(identity);activity('Session restored',identity.organizationId)}

const originalCreateWorkspaceRequest=createWorkspaceRequest;
 createWorkspaceRequest=async(e)=>{const form=e.currentTarget;await originalCreateWorkspaceRequest(e);if(!state.token||!state.principal)return;state.model=String(new FormData(form).get('model')||state.model);const quickInference=$('#quickInference');if(quickInference){quickInference.classList.add('card');$('[data-page="overview"]')?.append(quickInference);quickInference.onsubmit=runBoundedInference}persistSession();await activateWorkspace(state.principal,true);activity('Workspace created',state.principal.organizationId);$('#workspace').scrollIntoView({behavior:'smooth',block:'start'})};

$('#onboardingForm').onsubmit=async(e)=>{const button=e.currentTarget.querySelector('button[type="submit"]');if(button?.disabled)return;button.disabled=true;button.textContent='Creating workspace…';try{await createWorkspaceRequest(e)}finally{button.disabled=false;button.textContent='Create workspace'}};
$('#continueSession').onclick=async()=>{try{await connect($('#sessionToken').value,$('#sessionAgentToken').value)}catch(e){notice(e.message,true)}};
$('#recoverWorkspace').onclick=async()=>{const button=$('#recoverWorkspace');const invite=$('#workspaceRecoveryInvite').value.trim();if(!invite){notice('BETA_INVITE_REQUIRED',true);return}button.disabled=true;button.textContent='Recovering workspace access…';try{const key='recovery-'+Date.now()+'-'+Math.random().toString(36).slice(2,10);const response=await fetch('/api/v1/product/workspace-recovery',{method:'POST',headers:{'Idempotency-Key':key,'X-Fuse-Invite':invite},cache:'no-store'});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body?.error?.code||'CREDENTIAL_RECOVERY_FAILED');renderCredentialValues('#workspaceRecoveryResult',body);$('#workspaceRecoveryInvite').value='';notice('Replacement credentials issued once. Save them before leaving this tab.')}catch(err){notice(err.message,true)}finally{button.disabled=false;button.textContent='Recover workspace access'}};
$('#signOut').onclick=async()=>{const token=state.token;state.token='';state.agentToken='';state.agentId='';state.policyId='';state.providerConfigId='';state.productMandateId='';state.model='';state.lastInferenceRequestId='';clearPersistedSession();try{await fetch('/api/v1/session',{method:'DELETE',headers:{Authorization:'Bearer '+token},cache:'no-store'})}finally{location.reload()}};
function showView(view){const button=document.querySelector('[data-view="'+view+'"]');if(button)button.click()}
function scrollToInference(){const form=$('#quickInference');if(!form){showView('overview');return}showView('overview');form.scrollIntoView({behavior:'smooth',block:'center'});form.querySelector('textarea')?.focus()}
all('.rail button').forEach(btn=>btn.onclick=()=>{all('.rail button').forEach(x=>x.classList.toggle('active',x===btn));all('.view').forEach(v=>v.hidden=v.dataset.page!==btn.dataset.view)});all('[data-go-provider]').forEach(btn=>btn.onclick=()=>showView('providers'));all('[data-go-inference]').forEach(btn=>btn.onclick=scrollToInference);all('[data-step-view]').forEach(btn=>btn.onclick=()=>showView(btn.dataset.stepView));all('[data-step-inference]').forEach(btn=>btn.onclick=scrollToInference);all('[data-step-evidence]').forEach(btn=>btn.onclick=()=>showView('runs'));
async function loadProviders(){try{const {providers}=await api('/api/v1/product/provider-connections');const box=$('#providerList');if(!providers.length){box.className='empty';box.textContent='No active configuration. Save one to enable controlled inference.';return}box.className='';box.innerHTML='';providers.forEach(item=>{const row=document.createElement('div');row.className='record';const strong=document.createElement('strong');strong.textContent=item.provider+' · '+item.model;const span=document.createElement('span');span.textContent=item.id+' · credential v'+item.credentialVersion+' · '+item.status;row.append(strong,span);box.append(row)})}catch(e){notice(e.message,true)}}
$('[data-refresh="providers"]').onclick=loadProviders;
function formatAtomic(value){if(value===null||value===undefined||value==='')return'Unresolved';const raw=String(value);if(!/^\\d+$/.test(raw))return'Unavailable';const padded=raw.padStart(7,'0');const whole=padded.slice(0,-6)||'0';const fraction=padded.slice(-6).replace(/0+$/,'');return'$'+whole+(fraction?'.'+fraction:'')}
function appendDetail(parent,label,value){const item=document.createElement('div');item.className='detail-item';const caption=document.createElement('span');caption.textContent=label;const content=document.createElement('strong');content.textContent=value;item.append(caption,content);parent.append(item)}
function renderRunDetail(receipt){const detail=$('#runDetail');if(!detail)return;detail.innerHTML='';if(!receipt){const title=document.createElement('h3');title.textContent='Select a run';const copy=document.createElement('p');copy.textContent='Choose a receipt to inspect its decision, policy version, cost reservation, provider usage, and reconciliation state.';detail.append(title,copy);return}const title=document.createElement('h3');title.textContent=receipt.requestId;const copy=document.createElement('p');copy.textContent='Durable receipt readback for the active mandate.';detail.append(title,copy);const grid=document.createElement('div');grid.className='detail-grid';appendDetail(grid,'Execution',receipt.executionStatus||'No execution state');appendDetail(grid,'Decision',String(receipt.outcome||'unknown').toUpperCase());appendDetail(grid,'Policy',receipt.policyId+' · v'+receipt.policyVersion);appendDetail(grid,'Estimated',formatAtomic(receipt.estimatedCostAtomic));appendDetail(grid,'Reserved ceiling',formatAtomic(receipt.reservedCostAtomic));appendDetail(grid,'Provider usage',formatAtomic(receipt.actualCostAtomic));appendDetail(grid,'Reconciliation',receipt.reconciliationResolved?'Resolved':'No operator resolution');appendDetail(grid,'Reason codes',receipt.reasonCodes?.length?receipt.reasonCodes.join(' · '):'Authorized');detail.append(grid)}
function renderRunSummary(){const summary=$('#runSummary');if(!summary)return;const receipts=runState.receipts;let actual=0n;let actualCount=0;receipts.forEach(receipt=>{if(receipt.actualCostAtomic!==null&&/^\\d+$/.test(String(receipt.actualCostAtomic))){actual+=BigInt(String(receipt.actualCostAtomic));actualCount+=1}});const completed=receipts.filter(receipt=>receipt.executionStatus==='completed').length;const held=receipts.filter(receipt=>receipt.executionStatus==='held'||receipt.executionStatus==='executing').length;summary.textContent=receipts.length+' receipt'+(receipts.length===1?'':'s')+' · '+completed+' completed · '+held+' needs review · reported usage '+(actualCount?formatAtomic(actual.toString()):'unresolved')}
function renderRuns(){const list=$('#runList');if(!list)return;list.innerHTML='';renderRunSummary();if(!runState.receipts.length){const empty=document.createElement('div');empty.className='empty';empty.textContent='No receipts found for this mandate yet.';list.append(empty);renderRunDetail(null);return}runState.receipts.forEach(receipt=>{const button=document.createElement('button');button.type='button';button.className='run-record'+(runState.selected?.requestId===receipt.requestId?' selected':'');const head=document.createElement('div');head.className='run-record-head';const title=document.createElement('strong');title.textContent=receipt.requestId;const status=document.createElement('span');status.textContent=(receipt.executionStatus||receipt.outcome||'unknown').replaceAll('_',' ');head.append(title,status);const meta=document.createElement('div');meta.className='run-record-meta';const cost=document.createElement('span');cost.textContent='actual '+formatAtomic(receipt.actualCostAtomic);const reserved=document.createElement('span');reserved.textContent='reserved '+formatAtomic(receipt.reservedCostAtomic);const policy=document.createElement('span');policy.textContent='policy v'+receipt.policyVersion;meta.append(cost,reserved,policy);button.append(head,meta);button.onclick=()=>{runState.selected=receipt;renderRuns()};list.append(button)});renderRunDetail(runState.selected||runState.receipts[0])}
async function loadRuns(){const field=$('#runsMandateId');const mandateId=String(field?.value||state.productMandateId).trim();if(field&&mandateId)field.value=mandateId;if(!mandateId){runState.receipts=[];runState.selected=null;renderRuns();return}const button=$('#loadRuns');if(button){button.disabled=true;button.textContent='Loading…'}try{const result=await api('/api/v1/product/mandates/'+encodeURIComponent(mandateId)+'/receipts?limit=50');runState.receipts=Array.isArray(result.receipts)?result.receipts:[];runState.selected=runState.receipts[0]||null;renderRuns();activity('Run history loaded',mandateId+' · '+runState.receipts.length+' receipt'+(runState.receipts.length===1?'':'s'))}catch(error){const message=error instanceof Error?error.message:'RUN_HISTORY_UNAVAILABLE';const list=$('#runList');if(list){list.innerHTML='';const empty=document.createElement('div');empty.className='empty';empty.textContent='Run history unavailable: '+message;list.append(empty)}notice(message,true)}finally{if(button){button.disabled=false;button.textContent='Load runs'}}}
$('#loadRuns').onclick=loadRuns;$('#refreshRuns').onclick=loadRuns;
$('#copyIntegrationSnippet').onclick=async()=>{const snippet=$('#integrationSnippet');const button=$('#copyIntegrationSnippet');if(!snippet||!button)return;try{await navigator.clipboard.writeText(snippet.innerText);button.textContent='Copied';setTimeout(()=>button.textContent='Copy quickstart',1600)}catch{notice('Copy failed; select the snippet and press Ctrl+C.',true)}};
function formObject(form){return Object.fromEntries(new FormData(form).entries())}
$('#providerForm').onsubmit=async(e)=>{e.preventDefault();const form=e.currentTarget;const value=formObject(form);try{await api('/api/v1/product/provider-connections',{method:'POST',body:JSON.stringify(value)});state.model=String(value.model||'').trim();persistSession();form.elements.apiKey.value='';notice('Provider configuration saved.');activity('Provider saved',value.provider+' · '+value.model);await loadProviders();ensureQuickInference()}catch(err){notice(err.message,true)}};
$('#agentForm').onsubmit=async(e)=>{e.preventDefault();const value=formObject(e.currentTarget);try{await api('/api/v1/product/agents',{method:'POST',body:JSON.stringify(value)});notice('Agent registered.');activity('Agent registered',value.agentId)}catch(err){notice(err.message,true)}};
$('#credentialForm').onsubmit=async(e)=>{e.preventDefault();const value=formObject(e.currentTarget);try{const result=await api('/api/v1/product/agent-credentials',{method:'POST',body:JSON.stringify({...value,capabilities:['inference:invoke','mandates:read','receipts:read']})});$('#issuedCredential').textContent='Copy now: '+result.token;notice('Credential issued once.');activity('Credential issued',value.credentialId)}catch(err){notice(err.message,true)}};
function renderCredentialValues(selector,result){const box=$(selector);const prefix=selector==='#workspaceRecoveryResult'?'recovery':'replacement';box.innerHTML='<p>Save these values now. They will not be shown again.</p><div class="field"><label>Service credential — Restore existing admin session</label><input id="'+prefix+'ServiceCredential" type="text" readonly><button class="ghost" type="button" data-copy="#'+prefix+'ServiceCredential">Copy</button></div><div class="field"><label>Agent credential — inference</label><input id="'+prefix+'AgentCredential" type="text" readonly><button class="ghost" type="button" data-copy="#'+prefix+'AgentCredential">Copy</button></div><div class="field"><label>Recovery code</label><input id="'+prefix+'RecoveryCode" type="text" readonly><button class="ghost" type="button" data-copy="#'+prefix+'RecoveryCode">Copy</button></div>';$('#'+prefix+'ServiceCredential').value=result.serviceCredential.token;$('#'+prefix+'AgentCredential').value=result.agentCredential.token;$('#'+prefix+'RecoveryCode').value=result.recoveryCode;wireCopyButtons()}
function renderCredentialPackage(result){renderCredentialValues('#workspaceCredentialsResult',result)}
$('#issueWorkspaceCredentials').onclick=async()=>{const button=$('#issueWorkspaceCredentials');if(!state.principal?.organizationId)return;button.disabled=true;button.textContent='Issuing replacement access…';try{const result=await api('/api/v1/product/workspaces/'+encodeURIComponent(state.principal.organizationId)+'/credential-package',{method:'POST'});state.agentToken=result.agentCredential.token;persistSession();renderCredentialPackage(result);notice('Replacement credentials issued once. Save them before leaving this tab.');activity('Replacement credentials issued',state.principal.organizationId)}catch(err){notice(err.message,true)}finally{button.disabled=false;button.textContent='Issue replacement access'}};
$('#policyForm').onsubmit=async(e)=>{e.preventDefault();const v=formObject(e.currentTarget);const body={policyId:v.policyId,version:Number(v.version),mode:'enforce',allowedProviders:[v.allowedProvider],allowedModels:[v.allowedModel],requiredCapability:'inference:invoke',limits:{maxPerCallAtomic:v.maxPerCallAtomic,maxHourlyAtomic:v.maxDailyAtomic,maxDailyAtomic:v.maxDailyAtomic,maxRequestsPerMinute:30,maxInputTokens:Number(v.maxInputTokens),maxOutputTokens:Number(v.maxOutputTokens)},workloadClasses:v.includeWorkload?[{id:v.workloadClass,maxCostPerCallAtomic:v.maxClassPerCallAtomic,maxInvocationsPerBranch:Number(v.maxClassInvocations),aggregateBudgetAtomic:v.classBudgetAtomic,minimumInputTokens:1,shadow:{classPriorWindowSpendAtomic:v.classPriorWindowSpendAtomic,windowSeconds:900,targetMinimumObservations:3,siblingMinimumForScoring:2,siblingMinimumForIntervention:3,confidenceConstant:5,divergenceThresholdBps:Number(v.divergenceThresholdBps)}}]:[]};try{await api('/api/v1/product/policies',{method:'POST',body:JSON.stringify(body)});notice('Policy version published.');activity('Policy published',v.policyId+' · v'+v.version+' · '+(v.includeWorkload?v.workloadClass:'ceiling-only'))}catch(err){notice(err.message,true)}};
function mandateValue(){const form=$('#mandateForm');if(!form.reportValidity())return null;return formObject(form)}
$('#createMandate').onclick=async()=>{const v=mandateValue();if(!v)return;try{await api('/api/v1/product/mandates',{method:'POST',body:JSON.stringify({mandateId:v.mandateId,name:v.name,assetId:'usd-micros',maximumSpendAtomic:v.maximumSpendAtomic,policyId:v.policyId,policyVersion:Number(v.policyVersion),expiresAt:null})});notice('Draft mandate created.');activity('Mandate draft',v.mandateId)}catch(err){notice(err.message,true)}};
$('#assignMandateAgent').onclick=async()=>{const v=mandateValue();if(!v)return;try{await api('/api/v1/product/mandates/'+encodeURIComponent(v.mandateId)+'/agents',{method:'POST',body:JSON.stringify({agentId:v.agentId})});notice('Agent assigned.');activity('Mandate assigned',v.mandateId+' · '+v.agentId)}catch(err){notice(err.message,true)}};
$('#createMandateBranch').onclick=async()=>{const v=mandateValue();if(!v)return;try{const result=await api('/api/v1/product/mandates/'+encodeURIComponent(v.mandateId)+'/branches',{method:'POST',body:JSON.stringify({branchId:v.branchId,parentBranchId:v.parentBranchId||null,agentId:v.agentId,allowedWorkloadClasses:[v.workloadClass],maximumSpendAtomic:v.branchMaximumSpendAtomic,expiresAt:v.branchExpiresAt?new Date(v.branchExpiresAt).toISOString():null})});$('#delegationEvidence').textContent='Fuse control-plane hash: '+result.branch.delegationHash;notice('Immutable branch bound.');activity('Branch bound',v.branchId+' · '+v.workloadClass)}catch(err){notice(err.message,true)}};
$('#activateMandate').onclick=async()=>{const v=mandateValue();if(!v)return;try{await api('/api/v1/product/mandates/'+encodeURIComponent(v.mandateId)+'/transitions',{method:'POST',body:JSON.stringify({to:'active'})});notice('Mandate activated.');activity('Mandate active',v.mandateId)}catch(err){notice(err.message,true)}};
function renderDecisions(decisions){const box=$('#decisionList');box.innerHTML='';if(!decisions.length){box.innerHTML='<div class="empty">No decisions recorded for this mandate.</div>';return}decisions.forEach(item=>{const row=document.createElement('div');row.className='event';const b=document.createElement('b');b.textContent=item.result.outcome+' · '+item.requestId;const p=document.createElement('p');const scope=item.input.branchId?' · '+item.input.branchId+' / '+item.input.workloadClass:'';const exposure=item.input.exposure?' · maximum exposure '+item.input.exposure.maximumExposureAtomic+' · authority left '+item.input.exposure.remainingAuthorityAtomic:'';p.textContent='Policy '+item.policyId+' v'+item.policyVersion+scope+exposure+' · '+(item.result.reasonCodes.join(', ')||'Authorized');row.append(b,p);box.append(row)})}
function renderShadow(evaluations){const box=$('#decisionList');box.innerHTML='';if(!evaluations.length){box.innerHTML='<div class="empty">No sibling shadow evidence recorded for this mandate.</div>';return}evaluations.forEach(item=>{const row=document.createElement('div');row.className='event';const b=document.createElement('b');const status=item.wouldSignal?'SIGNAL — SHADOW ONLY':item.wouldSignalTarget?'SIGNAL — BELOW INTERVENTION CONFIDENCE':item.cohortShift?'COHORT SHIFT — SHADOW ONLY':'NO SIGNAL';b.textContent=status+' · '+item.branchId+' / '+item.workloadClass;const p=document.createElement('p');p.textContent=item.provider+' · '+item.model+' · cohort order #'+item.cohortOrdinal+' · '+item.status+' · target '+item.targetObservationCount+' · siblings '+item.comparableSiblingCount+' · weight '+item.siblingWeightBps+' bps · baseline '+item.effectiveBaselineAtomic+' · '+(item.signals.join(', ')||'No signal');row.append(b,p);box.append(row)})}
$('#evidenceForm').onsubmit=async(e)=>{e.preventDefault();const id=formObject(e.currentTarget).mandateId;try{const result=await api('/api/v1/admin/mandates/'+encodeURIComponent(id)+'/decisions');renderDecisions(result.decisions);activity('Decision evidence loaded',id)}catch(err){notice(err.message,true)}};
$('#loadShadow').onclick=async()=>{const form=$('#evidenceForm');if(!form.reportValidity())return;const id=formObject(form).mandateId;try{const result=await api('/api/v1/admin/mandates/'+encodeURIComponent(id)+'/shadow-evaluations');renderShadow(result.evaluations);activity('Sibling shadow loaded',id)}catch(err){notice(err.message,true)}};
$('#loadReconciliation').onclick=async()=>{try{const result=await api('/api/v1/admin/reconciliation');const box=$('#decisionList');box.innerHTML='';if(!result.cases.length){box.innerHTML='<div class="empty">No requests require reconciliation.</div>';return}result.cases.forEach(item=>{const row=document.createElement('div');row.className='event';const b=document.createElement('b');b.textContent='HOLD · '+item.requestId;const p=document.createElement('p');p.textContent=item.reasonCode+' · reserved '+item.reservedCostAtomic;row.append(b,p);box.append(row)})}catch(err){notice(err.message,true)}};
$('#reconciliationForm').onsubmit=async(e)=>{e.preventDefault();const value=formObject(e.currentTarget);const body={resolution:value.resolution,note:value.note,externalReference:value.externalReference,...(value.actualCostAtomic?{actualCostAtomic:value.actualCostAtomic}:{})};try{await api('/api/v1/admin/reconciliation/'+encodeURIComponent(value.requestId)+'/resolve',{method:'POST',body:JSON.stringify(body)});notice('Reconciliation resolved.');activity('Reconciliation resolved',value.requestId);e.currentTarget.reset();await loadRuns()}catch(err){notice(err.message,true)}};
void restoreStoredSession();
</script></body></html>`;
}
